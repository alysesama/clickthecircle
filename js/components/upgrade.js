// Sound Effects
const upgradeToggleSound = new Audio('res/sound/menu_toggle.mp3');

function initializeUpgrade() {
    // This function is now primarily for future upgrade-specific logic
    // that needs to run after the panel is loaded.
    // The button initialization is handled in loader.js
}
